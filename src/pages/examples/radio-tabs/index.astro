---
const title = "Radio Tabs";
import Layout from "../../../layouts/Layout.astro";
import { Code } from "astro:components";
---

<Layout title={title}>
  <iframe
    src={`${import.meta.env.BASE_URL}examples/radio-tabs/example/index.html`}
    style="width:100%; height: 300px;"></iframe>

  <p>
    This example uses radio buttons to switch between tabs, so no JavaScript is
    needed.
  </p>
  <p>
    It also uses a custom <a
      href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/tab-picker"
      >HTML element</a
    > (tab-picker and tab-content).
  </p>
  <p>The following CSS is used to hide inactive tabs:</p>
  <Code
    code={`body:has(#veni:not(:checked)) #tab-veni,
body:has(#vidi:not(:checked)) #tab-vidi,
body:has(#vici:not(:checked)) #tab-vici {
display: none;
}
`}
    lang="css"
  />

  <a
    class="button"
    href={`${import.meta.env.BASE_URL}examples/radio-tabs/example/index.html`}
    >View Example</a
  >
</Layout>
